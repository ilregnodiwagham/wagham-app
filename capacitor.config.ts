import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ilregnodiwagham.companion',
  appName: 'Il Regno di Wagham',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
